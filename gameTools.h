
#ifndef GAMETOOLS_H
#define GAMETOOLS_H

#include <time.h>
#include "tools.h"

void initListCards(int*);
void pickCards(int*,int*);
void printCards(int*);
int cardsNotEmpty(int*);
void sort(int*);

#endif
